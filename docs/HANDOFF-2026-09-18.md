# Handoff — 17 e 18/09/2026 (chat "Automatização VII")

Complementa `HANDOFF-2026-09-17.md` (mapa do sistema, decisões, armadilhas). Aqui só o que mudou nestes dois dias e o que ficou aberto.

## Deploys em `main` (todos com card na Ajuda)

| Commit | O quê |
|---|---|
| `732262d`, `acacf0b` | Agenda: escala única de layout (controles 34px, raios 6/8/12, espaços ×4); título + faixa de foco lado a lado; cards de uma linha; sem-prazo entra na coluna da lista (JS move o elemento pra dentro do grid e devolve antes de qualquer `innerHTML`); Semana/Mês com `minmax(0,1fr)` |
| `5c9f600` | `abrirDetalhe` também procura em `cacheFoco` — cards da faixa de foco não abriam |
| `da0344d` | Campo **Setor** no detalhe da tarefa (Agenda e `tarefas-engine.js`), com histórico da troca; valor desconhecido é preservado como opção |
| `ca54137` | **Tema claro etapa 1**: `assets/js/theme.js` importado pelo `auth-guard.js` (61 telas); `data-theme="light"` no `<html>`, escolha em `localStorage['mr_tema']`; tokens de página em `style.css` (`--fill-1/2/3`, `--line`, `--line-strong`, `--text-2`, `--ok/--warn/--err`, `--overlay`, `--pop`, `--topbar-bg`); convertidos: index, agenda, clientes/index, tarefas-engine, tour, ajuda. Chips com cor inline escurecem por `filter` no claro. Restam ~55 telas com cor fixa (`grep -oE '#[0-9A-Fa-f]{6}|rgba\(255,255,255' <tela> | wc -l` dimensiona) |
| `6179c62` | Migração `unifica_setor_dp`: `transicao_kit` e `tarefas` com `departamento_pessoal` → `dp`; `comercial-transicao.html` agrupa por `dp`. `clientes.setores` continua `departamento_pessoal` (outro conceito) |
| `73e1958` | Vários clientes por tarefa/regra recorrente: uma linha por cliente |
| `28c2022` | **`assets/js/nova-tarefa.js`** — formulário único de Nova tarefa (Agenda e setores). Sem abas: campo Repetir decide o destino (não → `tarefas`; repetir sem cliente → `rotinas`; mensal/anual com cliente → `tarefas_recorrentes` + primeira tarefa já criada com `regra_id`/`competencia`; diária/semanal com cliente → recusa). "Tarefa rápida" da Agenda removida. Modal interno do engine só edita rotina |
| `051d8f0`, `130ded5` | Lote nas listas da Agenda (busca e foco): caixa por tarefa, Selecionar todas, **Concluir N** com observação opcional e **Registrar andamento** em massa (obrigatório) — tudo via `tarefa_historico`. Busca de tarefas passou de 50 pra 200 |
| `f06c147` | Migração `clientes_nome_busca`: coluna gerada (nomes + documento, só `[a-z0-9]`, sem acento). Usada nos 5 pontos de busca de cliente. "L T C" agora é achado por "LTC" |
| `31043fa` | Seletor **Ordem** nas listas (prazo→cliente A–Z padrão; prazo→título; cliente; título), `localStorage['mr_agenda_ordem']` |

## Harness visual (não versionado, recriar quando precisar)
`/tmp/h`: cópia da tela + `assets/js/supabase.js` **mock** (Proxy encadeável com filtros eq/neq/is/lt/lte/gt/gte/in, insert/update/single, `todosClientes`, `auth.getUser/getSession`) + Playwright (Python) servindo em `:8765`. Tour se desliga com `localStorage['mr_tour_<chave>::<email>']='ok'`. **Cuidado:** copiar `assets/` do repo pro harness sobrescreve o mock — copiar arquivo a arquivo.

## Aberto, na ordem
1. Demandas da Adaini (contábil). Pendente de autorização: mover a linha de `carteira_info` de uma PF pra PJ do mesmo grupo e limpar `saida` inválida (detalhes na memória do projeto).
2. **Etapa 2 da unificação**: edge function diária gerando tarefas de `tarefas_recorrentes` (mensais e anuais; "Gerar tarefas do mês" some); diárias continuam como `rotinas` (decisão do Samuel); obrigações fiscais ficam fora.
3. Tema claro nas demais telas, conforme a fila de uso.
4. Revisor determinístico da análise técnica (adiado).
5. Skill do Simples no Code da Thalia e do Diego (fora do repo).
6. Caso de cliente (perfis fiscais em sistema de PDV, Simples/RS) — aguardando respostas do cliente; tabela pra validação do fiscal está na memória do projeto.
